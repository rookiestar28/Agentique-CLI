# Agent Assistant Starter

This starter shows a minimal public agent profile package. It is intended for authors who want a small example of resource metadata, public package files, and a simple agent description.

Upload, platform scan, moderation, publication, distribution state, and readback are handled by `agentique.io`. Local validation only checks that the package shape is ready for upload review.
