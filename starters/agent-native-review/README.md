# Agent-Native Review Starter

This starter shows static agent-native preparation metadata for local review.

The package demonstrates namespace coordinates, non-certifying provenance labels, source-only install guidance, public metadata boundaries, and fail-closed resolver intent. It is an inspectable example only. It does not install, execute, authenticate, start MCP servers, resolve live resources, publish resources, approve submissions, or certify safety.

