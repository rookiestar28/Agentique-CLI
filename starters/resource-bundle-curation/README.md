# Resource Bundle Starter

This starter shows a small bundle that groups public guidance files with a resource manifest. It is useful when authors want to submit supporting documentation with a package.

Upload, platform scan, moderation, publication, distribution state, and readback are handled by `agentique.io`. Local validation only checks that the package shape is ready for upload review.
